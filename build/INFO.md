
# build

This directory contains job submission scripts for building the containers in the ASPECT Pipeline.

The scripts use definition files from the `definitions` directory and ASPECT source code from the `src` directory to build containers in the `containers` directory.

